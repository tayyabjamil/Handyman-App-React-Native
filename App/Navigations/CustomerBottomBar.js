import React from 'react';
import {Text, View} from 'react-native';
import { ORANGE } from '../helper/Color';
import { SCREEN } from '../helper/Constant';

const BottomBar = () => {
  return (
    
        
          <View style={{height:70, width: SCREEN.width, justifyContent:'space-between', flexDirection:'row', alignItems:'center', backgroundColor:ORANGE.dark}}>
                
          </View>
       
  
  );
};
export default BottomBar;
